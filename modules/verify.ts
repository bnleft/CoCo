import { CoCoModule } from "../interfaces";

const VerifyModule: CoCoModule = {
    name: 'verify',
    permission: 'everyone',
    description: 'Verify user is a member',
    command(message, args) {

    }
}

export default VerifyModule;